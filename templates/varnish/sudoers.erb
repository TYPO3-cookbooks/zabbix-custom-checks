# Zabbix configuration for running checks without password
Cmnd_Alias ZABBIX_CMD_VARNISHSTAT = /usr/bin/varnishstat

# Disable syslog entries for ZABBIX_CMD_VARNISHSTAT
Defaults!ZABBIX_CMD_VARNISHSTAT !syslog

# ZABBIX special privileges
zabbix ALL=NOPASSWD: ZABBIX_CMD_VARNISHSTAT
